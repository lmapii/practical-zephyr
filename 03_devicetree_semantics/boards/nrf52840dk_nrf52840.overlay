// Empty board overlay.
//
// This overlay file is only picked up by the build process if:
// - the CMake variable DTC_OVERLAY_FILE is not set (or not set to this file)
// - nrf52840dk_nrf52840 is the chosen board.
//
// This is the recommended place for board overlay files (instead of placing them in
// the application's root directory) since it now also allows to specify additional
// revision files.